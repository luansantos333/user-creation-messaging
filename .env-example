# ================================================================================
# ENVIRONMENT CONFIGURATION TEMPLATE
# ================================================================================
#
# Instructions:
# 1. Copy this file to .env: cp .env-example .env
# 2. Replace all placeholder values with your actual configuration
# 3. Never commit the .env file to version control
#
# Required variables are marked with [REQUIRED]
# Optional variables have default values in docker-compose.yml
# ================================================================================

# ================================================================================
# DATABASE CONFIGURATION
# ================================================================================

# [REQUIRED] PostgreSQL database password
# Used by PostgreSQL container and application connection
DB_PASSWORD=your_secure_database_password_here

# [REQUIRED] PostgreSQL database username
# Default: java-user (if not set, but required by user-app service)
DB_USERNAME=java-user

# [OPTIONAL] PostgreSQL database name
# Default: java-db
# DB_NAME=java-db

# ================================================================================
# OAUTH2 / SECURITY CONFIGURATION
# ================================================================================

# [REQUIRED] OAuth2 Client ID
# Used for client authentication in the authorization server
CLIENT_ID=your-client-id

# [REQUIRED] OAuth2 Client Secret
# Strong secret for client authentication (use a secure random string)
CLIENT_SECRET=your-secure-client-secret-here

# [REQUIRED] OAuth2 Client Name
# Display name for the OAuth2 client
CLIENT_NAME=your-client-name

# [OPTIONAL] OAuth2 Redirect URI
# Default: http://127.0.0.1:8081/authorized
# REDIRECT_URI=http://127.0.0.1:8081/authorized

# [OPTIONAL] JWT Token Time-To-Live (in seconds)
# Default: 3600 (1 hour)
# TOKEN_TTL=3600

# ================================================================================
# EMAIL / SMTP CONFIGURATION
# ================================================================================

# [REQUIRED] SMTP username (email address)
# For Gmail, use your Gmail address
MAIL_USERNAME=your-email@example.com

# [REQUIRED] SMTP password or app-specific password
# For Gmail, generate an app password: https://myaccount.google.com/apppasswords
MAIL_PASSWORD=your-app-password-here

# [REQUIRED] SMTP server host
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
MAIL_HOST=smtp.gmail.com

# [REQUIRED] SMTP server port
# TLS: 587, SSL: 465
MAIL_PORT=587

# ================================================================================
# KAFKA CONFIGURATION (Advanced - Usually use defaults)
# ================================================================================

# [OPTIONAL] Kafka broker URI for user-app
# Default: kafka:9092
# KAFKA_URI=kafka:9092

# [OPTIONAL] Kafka broker URL for mail-app
# Default: kafka:9092
# KAFKA_URL=kafka:9092

# ================================================================================
# APPLICATION PROFILES
# ================================================================================

# [OPTIONAL] Spring profile for user-application
# Default: prod
# USER_APPLICATION_PROFILE=prod

# [OPTIONAL] Spring profile for mail-application
# Default: prod
# MAIL_APPLICATION_PROFILE=prod

# ================================================================================
# DATABASE CONNECTION (Advanced - Usually use defaults)
# ================================================================================

# [OPTIONAL] PostgreSQL port exposed to host
# Default: 5432
# POSTGRES_PORT=5432

# [OPTIONAL] JDBC URL for PostgreSQL
# Default: jdbc:postgresql://postgresql:5432/java-db
# POSTGRES_URL=jdbc:postgresql://postgresql:5432/java-db

# ================================================================================
# SECURITY NOTES
# ================================================================================
#
# - Use strong, randomly generated passwords for DB_PASSWORD and CLIENT_SECRET
# - For Gmail SMTP, enable 2FA and create an app-specific password
# - Never share or commit the .env file
# - Rotate credentials periodically
# - For production, consider using secrets management (e.g., Docker Secrets, Vault)
#
# ================================================================================